package controllers;


import play.mvc.Result;
import play.mvc.Results;

public class JavaController {
    public Result def() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result forSome() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result implicit() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result lazy() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result match() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result object() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result override() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result sealed() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result trait() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result type() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result val() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result var() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result with() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result yield() {
        return Results.redirect(controllers.routes.HomeController.index());
    }

    public Result export() {
        return Results.redirect(controllers.routes.HomeController.index());
    }
}
